<Window x:Class="WpfApp7.Info"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp7"
        mc:Ignorable="d"
        Title="Info" Height="450" Width="800">
    <Grid>
        <StackPanel>
            <Label>Vendor</Label>
            <TextBox Text="{Binding InfoCar.Vendor}" FontSize="30" ></TextBox>
            <Label>Model</Label>
            <TextBox Text="{Binding InfoCar.Model}" FontSize="30" ></TextBox>
            <Label>Year</Label>
            <TextBox Text="{Binding InfoCar.Year}" FontSize="30" ></TextBox>
        </StackPanel>
    </Grid>
</Window>
